<p><big>
  <label><%= @character.gender == 'female' ? 'Ms' : 'Mr' %>. <strong><%= @character.name %></strong></big>, a curious <%= @character.gender == 'female' ? 'lady' : 'gentleman' %></label>
</p>

<h3>Clues: <%= @character.clues %></h3>

<h4>Skills</h4>
<ul>
  <% @character.profile.find_tagging('skills').tags.each do |tag| %>
    <li><%= tag._id.titleize  %> <%= tag.value %></li>
  <% end %>
</ul>

<h4>Pathology</h4>
<ul>
  <% @character.profile.find_tagging('pathology').tags.each do |tag| %>
    <li><%= tag._id.titleize  %> <%= tag.value %></li>
  <% end %>
</ul>

<h4>Trappings</h4>
<p>Equip items to gain their benefits</p>
<div>
  <% if right_hand = @character.profile.trappings.get('right_hand')%> 
    <p>Right Hand: <%= right_hand.title %> <%= link_to '[unequip]', equipment_trapping_path(right_hand._id, 'right_hand'), :method => :delete %></p>
  <% else %>
    <p>Right Hand: empty</p>
  <% end %>

  <% if left_hand = @character.profile.trappings.get('left_hand')%> 
    <p>Left Hand: <%= left_hand.title %> <%= link_to '[unequip]', equipment_trapping_path(left_hand._id, 'left_hand'), :method => :delete %></p>
  <% else %>
    <p>Left Hand: empty</p>
  <% end %>
</div>

<h4>Equipment</h4>
<% if @character.profile.find_tagging('equipment').tags.blank? %>
  <p>You don't own any equipment. You can purchase items from various merchants and discover them during your investigations.</p>
<% else %>
  <ul>
    <% @character.profile.find_tagging('equipment').tags.each do |tag| %>
      <li>
        <%= tag._id.titleize %><br />
        <% if equipment = @equipment.select{|e| e._id == tag._id}.first %>
          <em><%= equipment.modifier_display %></em>
          <% if @character.profile.trappings.location_of(equipment._id).blank? %>
            <%= link_to '[equip]', equipment_trapping_path(equipment._id, equipment.location), :method => :put %>
          <% end %>
        <% end %>
      </li>
    <% end %>
  </ul>
<% end %>

<h4>Belongings</h4>
<ul>
  <% @character.profile.find_tagging('belongings').tags.each do |tag| %>
    <li><%= tag._id.titleize  %> <%= tag.value %></li>
  <% end %>
</ul>

<br />
<em>
  <h4>Cosmic Identifier:</h4>
  <blockquote><%= @character.user_id %></blockquote>
  <p>Give this to strangers who may need to find you in Cthulhu Casebook.</p>
</em>